apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: tak-operator-system
# One-time import into a NEW CNPG Cluster, with TAK and its operator stopped.
resources:
  - ../local
  - legacy-network-policy.yaml
patches:
  - path: import.yaml
